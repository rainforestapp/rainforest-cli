source 'https://rubygems.org'

# Specify your gem's dependencies in rainforest-cli.gemspec
gemspec

# Until API bits is updated
gem 'rainforest', github: 'epaulet/rainforest-ruby', branch: 'rf-test-rfml-id'

group :test do
  gem "rspec", "2.14"
end
